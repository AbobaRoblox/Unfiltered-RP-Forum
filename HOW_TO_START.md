# Как запустить сервер

## Пошаговая инструкция

### 1. Откройте терминал/командную строку в папке проекта

**Windows:**
- Нажмите `Shift + Правый клик` в папке проекта
- Выберите "Открыть окно PowerShell здесь" или "Открыть в терминале"

**Или:**
- Откройте PowerShell/CMD
- Перейдите в папку: `cd путь\к\папке\forum`

**На сервере (SSH):**
```bash
cd /var/www/u3372230/data
```

### 2. Установите зависимости (если еще не установлены)

```bash
npm install
```

Вы увидите процесс установки пакетов (express, mysql2, cors).

### 3. Проверьте настройки базы данных

Перед запуском убедитесь, что в файле `server-mysql.js` правильно указаны данные для подключения к MySQL:

```javascript
const dbConfig = {
    host: 'localhost',
    user: 'u3372230_default',  // Ваш пользователь БД
    password: '2zU57A3q7HdzliBz',  // Ваш пароль БД
    database: 'u3372230_unfilteredrp-bd'  // Имя базы данных
};
```

### 4. Запустите сервер

```bash
npm start
```

или напрямую:

```bash
node server-mysql.js
```

## Как понять, что сервер запущен? ✅

Когда сервер успешно запустится, вы увидите в консоли:

```
╔════════════════════════════════════════╗
║   СЕРВЕР УСПЕШНО ЗАПУЩЕН! ✓          ║
╚════════════════════════════════════════╝

🌐 Сервер работает на: http://localhost:3000
📡 Порты: 3000

✅ Подключение к MySQL успешно!
✅ API готов к работе

💡 Чтобы остановить сервер, нажмите: Ctrl + C
```

**Если вы видите эти сообщения - сервер работает! 🎉**

### Если видите ошибку подключения к MySQL:
- Проверьте, что MySQL сервер запущен
- Проверьте правильность данных подключения в `server-mysql.js`
- Убедитесь, что база данных создана и импортирован `database_dump.sql`

## Как проверить, что сервер работает

### Способ 1: Откройте браузер
1. Откройте браузер
2. Перейдите по адресу: `http://localhost:3000`
3. Должен открыться ваш форум

### Способ 2: Проверка в терминале
- Терминал должен быть **активным** (курсор мигает)
- Вы **НЕ видите** сообщений об ошибках
- Сервер **НЕ завершился** (окно не закрылось)

## Если что-то пошло не так ❌

### Ошибка: "npm: command not found"
**Решение:** Установите Node.js с https://nodejs.org/ (версия 14+)

### Ошибка: "Port 3000 is already in use"
**Решение:** 
- Закройте другое приложение, использующее порт 3000
- Или измените порт: `PORT=3001 npm start`

### Ошибка: "Cannot find module"
**Решение:** Выполните `npm install`

### Ошибка: "❌ Ошибка подключения к MySQL"
**Решение:**
1. Проверьте, что MySQL сервер запущен
2. Проверьте данные подключения в `server-mysql.js` (host, user, password, database)
3. Убедитесь, что база данных создана
4. Проверьте, что импортирован `database_dump.sql`

### Ошибка: "Access denied for user"
**Решение:** Проверьте правильность логина и пароля MySQL в `server-mysql.js`

## Как остановить сервер

1. Перейдите в окно терминала, где запущен сервер
2. Нажмите: `Ctrl + C` (Windows/Linux) или `Cmd + C` (Mac)
3. Сервер остановится

## Важные моменты

⚠️ **Сервер должен работать ПОСТОЯННО**, пока вы используете форум!

- Если закрыть терминал - сервер остановится
- Если остановить сервер - форум перестанет работать
- Для продакшена используйте PM2, systemd или CRON для автозапуска

## Настройка для работы с реальным сервером

После запуска сервера нужно настроить клиентскую часть:

1. Откройте браузер и откройте консоль разработчика (F12)
2. В консоли выполните:
```javascript
localStorage.setItem('useRealServer', 'true');
localStorage.setItem('apiBaseUrl', 'http://localhost:3000'); // или ваш домен
location.reload();
```

Теперь сайт будет работать с реальным сервером вместо LocalStorage!

**Для продакшена (на хостинге):**
- Замените `http://localhost:3000` на ваш реальный домен
- Например: `localStorage.setItem('apiBaseUrl', 'https://unfilteredrp-forum.ru');`

